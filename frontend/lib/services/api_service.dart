/*
  FILE: api_service.dart
  DESCRIPTION:
  Manages network communication with the Python Backend (FastAPI).
  Sends HTTP requests (POST/GET) to the API endpoints.
  Acts as the bridge between the Frontend UI and the RAG logic in the backend.

  REFERENCE:
  See Architecture: "Frontend (Ripple) sends query to backend (FastAPI)".
*/
